Read `START_HERE.md` first for any project-related work in this repository.

Then open `docs/ROUTER.md` and follow only the narrowest route required by the current task.

If the routed work concerns this repository as a project, read `PROJECT.md` next, then `PROJECT_INDEX.md` only if it is useful, then only the minimum additional files needed for the task.

Before broad repository scanning, inspect curated indexes such as `PROJECT_INDEX.md`, `blocks/PROJECT_INDEX.md`, `knowledge-library/PROJECT_INDEX.md`, and generated indexes under `indexes/` when they are relevant.

When wording is uncertain, the repository is large, or the correct files are not obvious, use the local semantic retrieval path described by `docs/AGENT_INDEX_FIRST_ENTRY_STANDARD.md`, then open the canonical files for any promising hits before relying on them.

Do not full-scan the repository by default when indexes or retrieval can narrow the file set first.

This repository is itself governed by `Project Execution OS`.

Do not duplicate standards into ad hoc files or rely on memory when the current repository documents can be read directly.

Follow `docs/EXISTING_SOLUTION_FIRST_STANDARD.md`:

- search before invention;
- adapt before rebuilding;
- create new central standards or mechanisms only when existing repository patterns and verified sources are not adequate.
